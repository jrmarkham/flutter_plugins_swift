import 'package:flutter/material.dart';
import 'package:plugins_swift/src/app.dart';

void main() {
  runApp(MaterialApp(
      title: 'Swift Channel',
      theme: ThemeData(primarySwatch: Colors.green),
      home: App()));
}
